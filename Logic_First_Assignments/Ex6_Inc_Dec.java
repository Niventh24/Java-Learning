public class Ex6_Inc_Dec {//increment decrement problems
    public static void main(String[] args) {
        // int a=3;
        // int b=4;
        // int c=6;
        // int d=4;
        // int p=0;
        //c = x++ + ++y + x + y++ + ++x + ++y;
        // c = a++ + ++b + a + b++ + ++a + ++b;

       // d = a-- - --b + ++a + b-- + a++ - --b + ++a;
        
        // p = a++ + ++b + c-- + --d + a + ++b - --c + d++;
        
        //p = ++a + b++ - --c + d-- + a + ++b + --c + d;
        
       // p = --a + b++ + ++c - d-- + a + --b + ++c - d++;
        
        //p = a++ + --b - c++ + ++d + --a + b-- - c + ++d;
        
        // b = --a + b++ - c-- + ++d + a + ++b - --c + d--;
        
        // c = a + b-- - ++c + d++ + --a + b - c++ + ++d;
        
        // d = --a + ++b - c++ + d-- + a++ + --b - c + ++d;
        
        // a = a + ++b - --c + d++ + a-- + b + --c - ++d;
        
        // b = a++ - --b + c-- - ++d + a - b++ + ++c - d--;
        
        // c = --a + b++ + --c - d++ + a + b-- - ++c + d;
        
        // d = a + ++b - c-- + d++ + a-- - b + ++c - d;
        
        // a = ++a + --b - c++ + d-- + --a + b++ - ++c + d;
        
        // b = a++ + --b - c-- + ++d + a - b++ + --c + ++d;
        
        // c = --a + b++ - c-- + ++d + a++ - --b + c + d++;
        
        // d = ++a - --b + c++ - d-- + a++ - b + ++c - --d;
        
        // a = a++ - b-- + ++c - d++ + --a + b - c++ + ++d;
        
        // b = --a + ++b - c++ + d-- + a++ - --b + ++c - d;
        
        // c = a++ - --b + c-- - ++d + --a + b++ - c + ++d;
         
       // System.out.println(p);
    }
    
}
